{
  "packageManager": "bun@1.4.0",
  "name": "tse-family-learning",
  "version": "1.0.0",
  "description": "LearningQuest — mobile-native HTML5 learning app for personalised practice",
  "scripts": {
    "build": "echo 'Static site ready'",
    "validate": "bun scripts/validate.mjs",
    "test": "bun run validate && bun scripts/assert-public-path.cjs",
    "smoke:mobile": "playwright test",
    "start": "node server.js"
  },
  "engines": { "bun": ">=1.4.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  }
}
